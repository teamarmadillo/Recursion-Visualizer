
// Contains treeNode used for creating recursion tree
// as well as algorithms for searching through and developing
// said trees
const espree = require("espree");
const ast = espree.parse()

// Recursive Call Tree Node
const RCTNode = (stringState) => {
    let value = stringState;
    let prev = null;
    let next = {};
}

// Given a base node containing the string grabbed from the state,
// recursively expand the tree for containing partitions of the
// string that represent each recursive call
const expand = (head) => {

}


